services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: text-extraction-api
    ports:
      - "8873:8873"
    volumes:
      - .:/app
    command: uvicorn --env-file .env app.main:app --host 0.0.0.0 --port 8873 --reload
